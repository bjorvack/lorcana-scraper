name: scrape
on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:
# TODO: implement per DESIGN.md → "scrape.yml — scheduled run".
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "scrape.yml stub"

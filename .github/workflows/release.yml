name: release
on:
  pull_request:
    types: [closed]
    branches: [main]
# TODO: publish tournaments-vN release on merge of a scrape/* PR.
jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo stub
